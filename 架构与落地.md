这里为你准备了两份独立且互补的文档。第一份是**架构设计方案**，侧重于系统拓扑、边界划分与核心机制；第二份是**落地实施流程**，侧重于工程推进的步骤、交付物与风险控制。
这两份文档摒弃了过度设计的华丽辞藻，力求科学、务实，直接指导代码开发。
---
# 文档一：Hermes Agent 架构设计方案
## 1. 架构定位与设计哲学
Hermes Agent 采用 **Wails + Go + Erlang/OTP** 的固定三进程架构。核心设计哲学是 **“单大脑强控，Go 纯执行，UI 与执行分离，三进程各司其职”**：
1. **固定三进程部署**：
   - **Wails 进程**：负责桌面窗口、前端渲染与用户交互。
   - **Wails 的 Go 进程**：负责 `brain.Bridge`、`HermesService`、工具执行与模型调用，是 UI 与 Erlang 之间的桥和执行层。
   - **Erlang 进程**：负责 `gen_statem` 编排、上下文组装、状态存储、容错与恢复，是系统唯一的大脑。
2. **Erlang 独占编排权**：Erlang 的 `gen_statem` 是系统唯一的控制中心。ReAct 循环的每一个步进、上下文的组装、并发调度与容错降级，全部在 Erlang 侧闭环。
3. **Go/Eino 纯粹执行**：Go 侧引入字节跳动 Eino 框架，但**严格剥夺其编排能力**。Eino 仅作为对接大模型 API 和工具生态的 SDK 适配器。Go 侧不保存状态，不内部循环，做到“问什么答什么”。
4. **分层契约通信**：
   - **Wails ↔ Wails-Go**：通过 Wails Runtime 完成桌面 UI 与 Go Service 间调用。
   - **Wails-Go ↔ panel_server**：长度前缀 Protobuf（4 字节大端 + `PanelFrame`），承载会话级 RPC（`start_session` / `send` / `brain_status` 等）及 `send` 流式 push。
   - **agent_fsm ↔ Go 执行层**：Protobuf 二进制（gpb 5.0 生成），承载细粒度原子请求（`LLMInferRequest` / `ToolExecRequest`）。
   各通道均通过防腐层隔离，避免 UI、执行层、编排层相互渗透。
## 2. 系统拓扑与职责边界
### 2.1 整体拓扑
```
Wails (UI 进程)
   │  Wails Runtime IPC
   ▼
Wails-Go (Bridge + HermesService + Eino/Eion 执行进程)
   │  TCP/UDS 连接池 (默认 4) — 长度前缀 Protobuf (panel.proto)
   ▼
panel_server (Erlang, gen_server)  ─── 端口文件地址发现 (bin/run/panel.addr)
   │  Erlang 内部调用
   ▼
agent_fsm (Erlang, gen_statem, ReAct 编排)
   │  cast 调用
   ▼
bridge_manager (Erlang, gen_server)
   │
   ▼
Go 执行层 (由 Wails 的 Go 侧拉起/承载)
```
三进程间的共同约束是不变的：**FSM 真相、上下文组装、Prompt phase、循环边界、容错降级都在 Erlang；Go 只执行原子请求；Wails 只负责交互与宿主能力。**
### 2.2 Erlang 侧 (编排大脑 - Agent-brains)
- **`hermes_brains_sup`**：顶层监督者，`rest_for_one` 策略。子进程启动顺序固定为 `timing_wheel → mnesia_store → state_store → bridge_manager → agent_sup → case_store → memory_summarizer`，任一前置崩溃后续全部重启；`case_store` 与 `memory_summarizer` 位于较后位置，避免其局部故障影响 FSM 主链路。
- **`timing_wheel`**：通用时间轮，基于 `erlang:start_timer` 驱动。系统所有周期/一次性定时事件统一经它调度（`add_periodic` / `cancel`），严禁在业务模块直接使用 `erlang:send_after`。
- **`mnesia_store`**：`gen_server` 包装的 Mnesia 中期存储。负责 ETS 表的磁盘快照与恢复（`snapshot/1` / `restore/1`），周期快照 tick 由 `timing_wheel` 投递。**Mnesia 生命周期完全由本模块管理**，不得写入 `.app.src` 的 applications 列表，避免 RAM-only schema 冲突。
- **`state_store`**：ETS 持有者。存放 FSM 状态机快照与短期对话历史。`init` 时从 `mnesia_store:restore` 加载磁盘快照，实现跨重启恢复。
- **`panel_server`**：`gen_server`。Wails-Go 侧的 Protobuf 面板入口（契约 `proto/panel.proto`）。`init` 时在 `127.0.0.1` 监听 ephemeral port，把 `127.0.0.1:<port>` 写入端口文件（默认 `bin/run/panel.addr`，供 Wails-Go 读取发现）并打印 `PANEL_PORT:<port>` 到 stdout。accept 循环每连接 `gen_tcp:controlling_process` 交给独立 handler 进程（`{packet, 4}` + `{active, once}` 异步读）；`dispatch` 在 spawn 中执行并通过 `{rpc_reply, Frame}` 回写，与 `{push_stream, Frame}` 在同一 receive 中 multiplex，不阻塞流式 push。由 `hermes_brains_app:serve/0` 在监督树之外单独启动。必要时可通过 `exec_agent/2` 把执行请求回送到 Go 侧处理，但这不改变三进程边界。
- **`panel_pb_codec`**：Panel 通道 Protobuf 防腐层。业务层只操作 Erlang Map；`pack_request` / `pack_response_*` / `pack_stream_*` / `unpack_frame` 与 Go 侧 `panel_codec.go` 对齐。
- **`Agent_Sup`**：`simple_one_for_one` 监督者，动态派发 `agent_fsm` 进程，每个会话一个 FSM。
- **`Agent_FSM`**：`gen_statem` 核心状态机。维护会话生命周期，执行 ReAct 循环流转（`idle` / `thinking` / `acting` / `observing`）。
- **`Context_Assembler`**：上下文组装器。负责 history 裁剪、分阶段 System Prompt 拼接、摘要/RAG/负面案例注入，组装完整 LLM 请求包。
- **`prompt_templates`**：提示词模板库。按 FSM 阶段（首轮、工具失败、断连、接近循环上限）提供 `phase_hint` 片段。
- **`Bridge_Manager`**：执行桥管理器。负责 Erlang 与 Go 执行层之间的调用分发、超时控制、重试与断连恢复。文档层面将其统一视为“Erlang 调 Go 执行层”的桥；具体可通过 TCP 连接池或 `panel_server:exec_agent/2` 回送到 Go 侧实现，但对上层 FSM 透明。
- **`pb_codec`**：Protobuf 防腐层。业务层只操作 Erlang Map，由该模块负责与 Protobuf Binary 的互转。
### 2.3 Go 侧 - Eion-tools (执行肌肉)
- **`Command_Dispatcher`**：监听 Erlang 节点的 TCP/UDS 连接，解码 Protobuf 帧后路由给具体的执行器。作为独立 server 进程运行（`cmd/server/main.go`）。
- **`Eino_Model_Wrapper`**：调用 Eino 的 `model.ChatModel` 接口。将 Erlang 传来的请求转为 API 格式，调用 LLM，解析响应后封装为 Protobuf 发回 Erlang。
- **`Eino_Tool_Wrapper`**：调用 Eino 的 `tools.Tool` 接口。执行具体的工具函数（如 HTTP 请求），返回结果。
- **统一 Capability Registry**：Go 侧已引入统一 `CapabilityDesc` 抽象，`tool / plugin / mcp / skill` 通过同一注册表暴露给 Erlang 与前端；`list_capabilities` 返回 `name / kind / source / version / streaming / risk_level / cost_hint / tags` 等结构化元数据，不再依赖按名称猜测来源。
- **`MCP Manager`**：已支持按配置拉起 stdio MCP server，完成 `initialize / tools/list / tools/call` 握手，并把远端 MCP 工具注册为统一 capability。
- **`Skill Runtime`**：已支持在 Go 侧以组合能力方式注册内建 skill；当前示例为 `workspace_briefing`，内部组合 `repo_map + code_search`，但不保存多轮真相。
- **`Eino_Memory_Wrapper`**（Phase 4.1 ✅）：封装 Eino `Embedder` + `Indexer`/`Retriever`（**Redis Stack 首选**），对外表现为 `memory_store` / `memory_search` / `memory_import` 三个模型可见 `Tool`，无内部 Agent 循环；另提供内部清理工具 `memory_purge_session` 供 `delete_session` 做 best-effort 记忆清除。
- **`Panic_Guard`**：全局异常拦截器。捕获 Go 侧执行过程中的 Panic，防止进程崩溃，将异常转为错误响应发回 Erlang。
### 2.4 Go 侧 - Wails-Go 宿主进程
- **`brain.Bridge`**：实现 Wails v3 `Service` 接口（`ServiceStartup` / `ServiceShutdown`）。维护到 `panel_server` 的 TCP/UDS 连接池（默认 4）；每连接 **reader goroutine 持续读帧** + **reqQ 异步写**，按 req id 路由 `PanelResponse`；`PanelStream` 经 `panel:stream` 事件异步推前端（`send` RPC 收到 `stream_id` 即返回，不阻塞等 final）。地址发现优先级：`HERMES_PANEL_ADDR` env > `HERMES_PANEL_ADDR_FILE` env > 默认端口文件 `<repo>/bin/run/panel.addr`。
- **`HermesService`**：Wails 绑定层，把前端调用转译为 `brain.Bridge.Call(method, args)`（底层 TCP + Protobuf，对前端仍暴露 JSON 字段的 Go struct）。
- **`Router/EionExecutor`**：位于 Wails-Go 进程内，承载模型调用与工具执行，是“Wails 的 Go 进程”中的执行层主体。
## 3. 通信基座设计：双通道防腐层
跨进程通信采用两条独立的契约通道，确保各侧无法自主决定后续动作。
### 3.1 通道 A：Wails-Go ↔ panel_server (Protobuf `panel.proto`)
- **帧格式**：4 字节大端长度前缀 + `PanelFrame` Protobuf 二进制。
- **顶层消息**：`PanelFrame` oneof — `PanelRequest`（client→server）/ `PanelResponse`（一次性响应）/ `PanelStream`（`send` 流式 push，chunk / tool_event / final / error）；实现层面也可扩展 `PanelExec` / `PanelExecResult` 用于把执行请求回送到 Go 侧。
- **请求**：`PanelRequest { id, method, args_bytes }`，`args_bytes` 为各方法 Args message 的嵌套 Protobuf（如 `StartSessionArgs`）。
- **响应**：`PanelResponse { id, result_bytes | error }`，`result_bytes` 为各方法 Result message 的嵌套 Protobuf。
- **执行扩展**：`PanelExec { id, agent_req_bytes }` 与 `PanelExecResult { id, agent_resp_bytes | error, terminal }`。这是 Erlang 与 Wails-Go 之间的执行回送能力，属于实现细节，不改变文档中的三进程边界。
- **编解码**：
  - Erlang：`panel_pb_codec.erl`（gpb 从 `proto/panel.proto` 生成 `panel.erl`）；业务层严禁直接调用 `panel` 模块。
  - Go：`internal/brain/panel_codec.go` + `proto/gen/panelpb`（`protoc-gen-go`）。
- **契约位置**：`Agent-brains/proto/panel.proto` 与 `Wails-v3/proto/panel.proto` 保持同源。
- **支持方法**：`start_session` / `send` / `list_tools` / `list_capabilities` / `debug_capability` / `approve` / `brain_status` / `get_history` / `delete_session` / `stop`（`approve` 仍为占位）。
### 3.2 通道 B：agent_fsm ↔ Eion-tools (Protobuf)
跨语言通信采用细粒度的原子请求，确保 Go 侧无法自主决定后续动作。
#### 3.2.1 核心协议定义 (`proto/hermes.proto`)
- **`LLMInferRequest`**：纯模型推理请求。包含 Erlang 拼装好的完整 `messages` 和 `tools` 描述，`model` / `api_base` / `api_key` 由请求携带（Go 侧不缓存 provider 配置）。
- **`ToolExecRequest`**：纯工具执行请求。包含 `req_id`（幂等键）、`tool_name` 和 `arguments_json`（JSON 字符串）。
- 顶层使用 `AgentRequest` / `AgentResponse` 的 `oneof payload` 区分 LLM 推理与工具执行。
#### 3.2.2 Erlang 侧 Map 隔离
业务逻辑层**严禁**直接操作 Protobuf 生成的结构。`pb_codec` 模块提供透明转换：
- `rebar3_gpb_plugin` 编译期从 `proto/hermes.proto` 生成 `src/app/hermes_pb.erl` 与 `include/hermes.hrl`。
- gpb 5.0 选项：`maps`（消息与字段均用 Map）、`mapfields_as_maps`、`maps_unset_optional = omitted`、`maps_oneof = flat`、`maps_key_type = atom`、`strings_as_binaries`。
- `pb_codec` 内部实现 `encode_req/1` / `decode_resp/1`，自动将普通 Erlang Map 与 Protobuf Binary 互转。
- **效果**：`Agent_FSM` 永远只处理 `#{model => <<"gpt-4">>, messages => [...]}`，完全感知不到 Protobuf 的存在。
## 4. 四层工程落地机制

Hermes 将 Agent 编排拆为四个正交工程面，均在 **Erlang 侧闭环**；Go 仅执行原子 LLM/Tool 请求。

| 工程面 | 核心模块 | 落地状态 |
|--------|----------|----------|
| **Loop Engineering** | `agent_fsm` | ✅ 高 |
| **Context Engineering** | `context_assembler`, `state_store`, `memory_summarizer`, `memory_rag` | ✅ 高 |
| **Prompt Engineering** | `prompt_templates`, `context_assembler` | ✅ 中→高 |
| **Harness Engineering** | `hermes_brains_sup`, `bridge_manager`, `panel_server`, `pb_codec` | ✅ 高 |

### 4.1 Loop Engineering (循环工程) - Erlang 主导
`Agent_FSM` 维持严格的状态流转：
- **`thinking`**：经 `bridge_manager:call_llm/2` 向 **Eion-tools** 异步发送 `LLMInferRequest`（Eino `ChatModel` 在 Go 侧调 LLM API），等待 `{llm_response, Ref, Resp}` 回链。
- **`acting`**：若决策包含工具调用，Erlang 经 `bridge_manager:call_tool_batch/2` **并行**派发多个 `ToolExecRequest`（每条 ToolCall 一条异步请求，响应按 `tool_call_id` 回投 `{tool_result, ToolCallId, Resp}`）。
- **`observing`**：收集所有工具结果，拼接进上下文，循环回 `thinking` 或输出最终结果。
- **循环上限**：`?MAX_LOOPS = 10`；`loop_count` 接近上限时触发 `near_loop_limit` 提示词阶段（见 §4.3）。

### 4.2 Context Engineering (上下文工程) - Erlang 主导
- **短期状态**：`state_store` 持有的 ETS 表存储当前状态机快照与最近 N 轮原始对话。
- **中期记忆**：`mnesia_store` 周期性把 ETS 快照落盘（默认 60s，由 `timing_wheel` 调度），跨重启可恢复。
- **长期记忆（摘要）**：Erlang 侧 `memory_summarizer` 在会话轮次结束或 `history_len` 超阈值时，用 LLM（经 Eion-tools `LLMInferRequest`）生成摘要，写入 Mnesia `session_summaries` 表；`context_assembler` 在 `thinking` 前按 session_id 注入最近摘要（非向量，低成本）。
- **长期记忆（向量 / RAG）**：向量索引与检索 **只在 Eion-tools** 完成（Eino `Indexer` / `Retriever`），Erlang **不部署向量库**；通过 ReAct 工具 `memory_search` / `memory_store` / `memory_import` 调度；`memory_rag` 在 `thinking(enter)` 前同步 `memory_search` 预取，`context_assembler` 注入 `memory_snippets` 段。
- **History 裁剪（✅ Step 4.2）**：
  - `filter_llm_history/1`：去掉 history 中的 `system` 角色，避免与组装器生成的单条 system 重复；
  - `trim_history/2`：默认保留最近 **40** 条 user/assistant/tool 消息（可通过 `max_history_msgs` 覆盖）；
  - `panel_server:start_session` 将会话级 `SystemPrompt` 经 `session_prompt` 传入 FSM，**不再**写入 history。
- **中期摘要阈值（✅ Step 4.2）**：`memory_summarizer:schedule_if_long/1` — 当 `state_store` 中 history ≥ 20 条时，在 `thinking(enter)` 异步触发 LLM 摘要（与轮次结束 `schedule/1` 并存，内部按 session 去重）。
- **组装流水线**：`context_assembler:build/2` 在每次 `thinking` 前，按优先级组装单条 System Prompt：
  1. `prompt_templates:core/0`（ReAct 基线）
  2. `prompt_templates:phase_hint/1`（FSM 阶段提示，见 §4.3）
  3. `session_prompt`（面板 `start_session` 自定义指令）
  4. `session_summaries` → `memory_snippets` → `failure_cases`
  5. 裁剪后的 history + tools 描述

### 4.3 Prompt Engineering (提示词工程) - Erlang 主导 ✅ *Step 4.2 已落地*

`prompt_templates.erl` 维护分阶段模板；`agent_fsm:detect_prompt_phase/1` 在 `thinking(enter)` 检测当前阶段并传入 `context_assembler`：

| 阶段 (`phase`) | 触发条件 | 提示意图 |
|----------------|----------|----------|
| `first_turn` | `loop_count = 0` | 首轮先理解意图，再决定是否调工具 |
| `after_tool_error` | 最近一条 `tool` 消息含 error/not found | 读错误信息，修正参数或换工具 |
| `after_bridge_disconnect` | 最近 system 含 Bridge disconnected | 说明断连，用已有上下文继续或给建议 |
| `near_loop_limit` | `loop_count >= max_loops - 1` | 尽快给出最终答案，避免新工具调用 |
| `thinking` | 默认 | 不追加额外片段 |

**模块路径**：`Agent-brains/src/util/erl/prompt_templates.erl`、`context_assembler.erl`；FSM 字段 `#data.session_prompt`。

### 4.4 Harness Engineering (编排 harness) - Erlang 主导

系统运行 harness——监督树、双通道 IPC、防腐层与工具发现，确保 Loop/Context/Prompt 在 Erlang 内闭环：

- **监督树**：`hermes_brains_sup`（`rest_for_one`）→ `timing_wheel` / `mnesia_store` / `state_store` / `bridge_manager` / `agent_sup` / `case_store` / `memory_summarizer`；`agent_fsm` 按会话 `transient` 重启并可从快照续跑。
- **双通道 IPC**：Wails-Go ↔ `panel_server`（`panel_pb_codec`）；FSM ↔ Go 执行层（`pb_codec` + `bridge_manager`，必要时可经 `PanelExec` / `PanelExecResult` 回送到 Go 侧）。
- **工具发现**：`panel_tools:fetch_tool_descs/0` 经 `bridge_manager:list_tools/1` 动态同步 Go 侧注册表（失败时回退 `default_tool_descs/0`）。
- **同步工具调用**：`bridge_manager:call_tool_sync/2` 供 `memory_rag` 等在 `thinking(enter)` 阻塞预取（超时可控，失败不阻断 FSM）。
- **详细容错**：见 §5。

## 5. 容错与降级策略
1. **进程级容错**：`hermes_brains_sup` 采用 `rest_for_one` 策略。`timing_wheel` 崩溃 → 后续全部重启重新注册 timer；`mnesia_store` 崩溃 → 磁盘层失效，后续重启；`state_store` 崩溃 → ETS 表丢失，FSM 及其下游全部重启（重启后从 mnesia 恢复快照）。Agent 进程崩溃，`Agent_Sup` 毫秒级重启。Go 侧无状态，重启不影响全局。
2. **连接池兜底**：`bridge_manager` 与 `brain.Bridge` 均维护连接池。单连接断开仅影响该连接在途请求，其余连接继续工作；断连后异步 `reconnectLoop` 轮询重连直到池满。
3. **通信层降级**：Eion-tools 节点全断时，`bridge_manager` 向所有 pending 的 `Agent_FSM` 发送 `{bridge_disconnect}`；`Agent_FSM` 收到后生成特殊 Observation 喂给 LLM 进行降级处理（致歉、重试或换路径）。
4. **柔性降级**：中断或工具失败不直接结束流程，而是作为特殊 Observation 喂给 LLM，由 LLM 决定道歉、重试或换路径。
5. **幂等性保障**：Erlang 生成全局 `ReqId`。Go 侧执行带副作用工具前，基于 `ReqId` 查去重缓存，防止重试导致脏数据。
6. **宏观循环上限**：Erlang 侧设置 ReAct Loop 最大次数限制（如 10 次），防止 LLM 陷入死循环消耗 Token。

## 6. 外部 MCP 能力接入（以 codegraph 为例）

Hermes 的持久化记忆已由 Agent-brains 的 `mnesia_store` + `memory_rag`（向量库）+ Eion-tools 的 `internal/memory` 三层覆盖，**不引入外部记忆服务**。外部 MCP server 只在"提供现有 capability registry 缺失的新能力"时才接入，避免与既有模块职责重叠。

**反例（已否决）**：agentmemory 等独立记忆服务会与 `memory_rag` / `state_store` 职责重叠，且引入跨 VM 依赖，不接入。

**正例（已采纳）**：codegraph（v1.1.1）提供代码语义图查询（callers/callees/impact/explore），是 `code_search` plugin 不具备的能力，经 MCP 接入。

### 6.1 codegraph 部署

- **位置**：Windows 本机（`C:\Users\Administrator\AppData\Local\codegraph\current\bin\codegraph.cmd`）
- **索引存储**：`e:\light-code-ai-1\.codegraph\codegraph.db`（SQLite WAL，已 gitignore）
- **索引规模**：54 文件 / 1200 节点 / 2651 边（主要为 Eion-tools Go 代码）
- **daemon**：named pipe `\\.\pipe\codegraph-0104951bbf0d09d4`，idle timeout 5min 自动退出，按需拉起

### 6.2 接入方式

经 Eion-tools MCP client manager（P1-007）接入，配置写入 `HERMES_MCP_SERVERS_JSON`：

```json
{"name":"codegraph","command":"codegraph","args":["serve","--mcp"],"enabled":true,"connect_timeout_ms":60000,"retry_delay_ms":1000}
```

- `codegraph serve --mcp` 作为 stdio MCP server，由 Eion-tools `internal/mcp` 拉起
- codegraph 的 `codegraph_explore` / `codegraph_node` / `codegraph_callers` 等工具注册为统一 capability，`kind=mcp`、`source=codegraph`
- 复用 P1-007/P1-008 链路，不新增协议

### 6.3 边界与治理

- **不持有会话真相**：codegraph 是只读代码索引，不参与 FSM / ReAct 循环
- **受 P0-004 裁剪策略治理**：经 MCP 接入后以 `kind=mcp` 纳入 capability registry，受 `capability_selector` / `capability_policy` 统一治理
- **与 `code_search` plugin 的关系**：`code_search`（Eion-tools 内置 plugin）做关键词级检索；codegraph 做语义图遍历（caller/callee/impact），二者互补不冲突
- **本地优先**：codegraph 是单 Rust 二进制 + SQLite，无外部依赖，索引数据不入仓库

---
# 文档二：Hermes Agent 落地实施流程文档
## 1. 实施总则
本流程按“先契约后逻辑，先单点后闭环，先跑通后容错”的科学路径推进。每个阶段设定明确的交付物与验收标准，确保代码增量可控，避免后期大规模返工。
## 2. 阶段一：通信基座与单点验证 (Phase 1)
**进度**：Step 1.1–1.4 已落地；Step 1.3 联调脚本 `scrtps/run-bridge-ping.bat` 已就绪（需先起 Eion-tools + 本地 `api-key.json`）。面板流式 push 已接 Wails `panel:stream` 事件。
**目标**：打通两条通信通道（Erlang ↔ Eion-tools 的 Protobuf、Wails ↔ panel_server 的 Protobuf），验证 Eino 作为纯执行 SDK 的可用性。
*   **Step 1.1: 定义 Protobuf 契约**
    *   **动作**：建立项目级 `proto/hermes.proto` 文件。定义 `AgentRequest` 和 `AgentResponse` 包装体，内部使用 `oneof` 区分 `LLMInferRequest` 和 `ToolExecRequest`。`Message` / `ToolCall` / `ToolDesc` 等消息已明确字段（`arguments` 以 JSON 字符串承载，与 OpenAI tool_call 对齐）。
    *   **验收**：`.proto` 文件通过 `protoc` / `gpb` 编译无报错。
*   **Step 1.2: Erlang 防腐层落地**
    *   **动作**：在 `rebar.config` 中引入 `rebar3_gpb_plugin` 与运行期依赖 `gpb 5.0.0`、`lager 3.9.2`。配置 gpb 选项 `maps` / `mapfields_as_maps` / `strings_as_binaries` 等。编写 `pb_codec.erl` 模块，实现 `encode_req/1` 和 `decode_resp/1`。
    *   **验收**：编写 EUnit 测试用例，传入一个包含嵌套 Map 的 Erlang 数据结构，`encode_req` 后立即 `decode_resp`，能还原出完全相等的 Map。
*   **Step 1.3: Go 侧 Eino 适配与单点联调** *脚本就绪*
    *   **动作**：初始化 Go 项目（Eion-tools），引入 Eino SDK。编写 `cmd/server/main.go` 作为独立 server，listen TCP(Windows)/UDS(Unix)。实现 `Eino_Model_Wrapper`，调用 Eino 的 `model.ChatModel` 接口对接 LLM API。
    *   **验收**：Erlang 端构造一个纯文本问答 Map -> 经 `bridge_manager` 连接池编码发给 Go -> Go 调通 LLM API -> 返回结果解码给 Erlang -> Erlang 控制台打印出 LLM 的回答。
    *   **联调**：`scrtps/start-tools.bat` → `scrtps/run-bridge-ping.bat`（或 rebar3 shell 中 `agent_demo:run/0` 跑完整 ReAct）。需本地 `api-key.json`。
*   **Step 1.4: panel_server 面板通道** ✅ *已落地并扩展*
    *   **动作**：定义 `proto/panel.proto`（`PanelFrame` / 各方法 Args·Result / 流式 `PanelStream`）。实现 `panel_server.erl` + `panel_pb_codec.erl`：`init` 时 listen ephemeral port，写 `bin/run/panel.addr`，accept + 每连接 handler（socket 所有权经 `controlling_process` 移交）。当前已实现 `start_session` / `send` / `list_tools` / `list_capabilities` / `debug_capability` / `brain_status` / `get_history` / `delete_session` / `stop`；Go 侧 `brain.Bridge` + `panel_codec.go`：连接池 + worker 多路复用。
    *   **验收**：✅ `Bridge.Call("start_session", ...)` 返回 `session_id`；✅ `Bridge.Call("brain_status", ...)` 返回 FSM 状态（如 `idle`）；✅ `panel_capability_e2e` 可校验 capability 元数据；✅ `panel_full_e2e` 可校验旧 `list_tools/start_session/send/panel:stream/get_history/delete_session` 全链路。
    *   **现状**：`list_tools` 动态同步 Eion-tools 注册表 ✅；`list_capabilities` / `debug_capability` 已成为能力平台调试入口；`approve` 仍为占位
## 3. 阶段二：Erlang 核心状态机闭环 (Phase 2)
**目标**：在 Erlang 侧实现完整的 ReAct 循环逻辑，跑通带工具调用的单会话 Agent。
*   **Step 2.1: 状态机骨架构建** ✅ *已落地*
    *   **动作**：基于 `gen_statem` 实现 `agent_fsm.erl`。定义 `idle`、`thinking`、`acting` 四个状态（`observing` 内联在 `acting` 收齐工具结果后）。
    *   **验收**：✅ 手动/面板 `send` 可驱动 FSM 在 idle/thinking/acting 间迁移；`brain_status` 可读当前状态。
*   **Step 2.2: 上下文组装器实现** ✅ *已落地*
    *   **动作**：实现 `context_assembler.erl`。提供接口将 System Prompt、最近对话历史、可用工具描述、失败案例拼接成 LLM 请求 Map（含 `stream => true`）。
    *   **验收**：✅ EUnit `context_assembler_tests`；组装 Map 经 `pb_codec` 编码后 Go 侧可直接作为 Eino 入参。
*   **Step 2.3: 并行工具调度与闭环** ✅ *已验收*
    *   **动作**：Go 侧 `get_weather` 工具；Erlang `acting` 经 `bridge_manager:call_tool_batch/2` 并行派发；`start_session` 注入 `panel_tools:default_tool_descs()`；`get_history` RPC + 前端对话同步。
    *   **验收**：✅ 用户提问「北京天气如何」→ 自动调工具 → LLM 总结。联调：`scrtps/run-agent-demo.bat` 或 `agent_demo:run/0`（需 `api-key.json` + `start-tools.bat`）。
## 4. 阶段三：容错机制与状态恢复 (Phase 3)
**进度**：Step 3.1–3.3 均已验收（FSM 崩溃恢复 / Eion-tools 断连降级 / Go panic+幂等）。
**目标**：达到生产级可用性，解决崩溃、断线、死循环等异常情况。
*   **Step 3.1: Erlang 进程级崩溃恢复** ✅ *已验收*
    *   **动作**：`Agent_FSM` 每次状态 enter 落 `{StateName, #data{}}` 快照到 `state_store`；`agent_sup` 改为 `transient` 异常重启；`init` 从快照恢复至 `idle/thinking/acting` 并 `register_session` 续跑。
    *   **验收**：`thinking` 中 `exit(Pid, kill)` 后监督者重启 FSM，新进程回到 `thinking` 并重新向 Eion-tools 投递 `LLMInferRequest`。联调：`scrtps/run-fsm-crash-test.bat`。
*   **Step 3.2: 通信层容错与柔性降级** ✅ *已验收*
    *   **动作**：`bridge_manager` 连接池 + `tcp_closed` / 超时 → `{bridge_disconnect}` cast 至 FSM；FSM `thinking`/`acting` 生成 observation 降级。Wails `brain.Bridge` 连接池 + `reconnectLoop`。**LLM 调用在 Eion-tools**，断连时 Erlang 不直连 API，只收 bridge 降级事件。
    *   **验收**：✅ `thinking` 等待 Eion-tools LLM 响应时强杀 `eion-tools-server.exe`，Erlang 不崩溃、历史注入 Bridge disconnected observation。联调：`scrtps/run-bridge-disconnect-test.bat`（测试后需重新 `start-tools.bat`）。
*   **Step 3.3: Go 侧 Panic 防护与幂等** ✅ *已验收*
    *   **动作**：`dispatcher` 含 `defer/recover`（`LLMInferRequest` / `ToolExecRequest` 均在 Eion-tools 执行）；`ToolExecRequest.req_id` 幂等 LRU 缓存（`sync.Map`）。Erlang `bridge_manager:call_tool` 以 `tool_call.id` 作 `req_id` 下发。
    *   **验收**：✅ `go test ./internal/dispatcher/...` — 工具 panic → 结构化 `error` 响应；相同 `ReqId` 仅执行一次。汇总：`.\make.ps1 test`（现已串联 `go test ./...` + `rebar3 eunit` + `panel_capability_e2e` + `panel_full_e2e`）。
## 5. 阶段四：长期记忆与产品化打包 (Phase 4)
**目标**：补齐 RAG 能力，完成 Mac 桌面端应用交付。
*   **Step 4.1: 长期记忆集成** *4.1a–4.1c 已落地*
    *   **原则**：遵守「Erlang 编排、Go 执行」—— **禁止** 在 Eion-tools 使用 `agent.NewAgent` / 完整 Eino RAG Chain；仅使用 Eino 原子组件 `Embedder` + `Indexer` + `Retriever`，封装为 `tools.Tool`（与 `get_weather` 同级）。
    *   **向量库选型（与 Eino 适配优先）**：
        | 优先级 | 场景 | 向量库 | eino-ext 包 | 说明 |
        |--------|------|--------|-------------|------|
        | **推荐** | 桌面 / Wails 单机、Phase 4 首版 | **Redis Stack 7.4+**（RediSearch 向量） | `components/indexer/redis` + `components/retriever/redis` | CloudWeGo **官方**实现；单二进制、Windows 可随 `bin/run/` 部署；与现有「独立进程 + 地址文件」模式一致（可增 `redis.addr`） |
        | 扩展 | 语料 >10 万 chunk、要 BM25+稠密混合检索 | **Milvus 2.x** | `components/indexer/milvus2` + `components/retriever/milvus2` | 官方支持 hybrid / sparse；需 Docker 或独立 Milvus 进程，适合后期上云 |
        | 备选 | 已有 ES 集群 | Elasticsearch 8 | `indexer/es8` + `retriever/es8` | 运维成本高，非桌面首选 |
        | 暂不选 | 零进程依赖、极小语料 | sqlite-vec 等 | **无官方 eino-ext** | 需自实现 `Indexer`/`Retriever` 接口，维护成本高，仅作极端离线备选 |
        | **禁止** | — | Erlang 侧向量引擎 | — | 与 Eino 生态脱节，破坏三进程边界 |
    *   **Embedding**：`eino-ext/components/embedding/openai`（或 DeepSeek 兼容 OpenAI embedding 端点）；维度与 Redis/Milvus index 创建时 **必须一致**（如 `text-embedding-3-small` → 1536）。凭证与 LLM 相同，从 `api-key.json` / 请求携带，**不在 Go 进程长期缓存 api_key**。
    *   **Go 侧工具（Eion-tools 注册）**：
        - `memory_store` — `{session_id, text, metadata_json}` → `Indexer.Store`（写入向量库；`req_id` 幂等）
        - `memory_search` — `{query, session_id?, top_k?}` → `Retriever.Retrieve` → 返回 `result_json`（命中片段列表）
        - `memory_import` — `{session_id, document_text, chunk_size?, source?}` → 分块批量 `Store`
        - `memory_purge_session` — `{session_id}` → 清理该会话写入的长期记忆；**内部工具**，仅供 `delete_session` 做 best-effort 清除，不暴露给 LLM
    *   **Erlang 侧（Agent-brains）**：
        - `panel_tools` 增加上述工具描述，供 LLM 在 ReAct 中按需调用；
        - `memory_summarizer.erl`：轮次结束后异步摘要 → Mnesia（结构化长期记忆，非向量）；
        - `memory_rag.erl` + `bridge_manager:call_tool_sync/2`：`thinking(enter)` 前预取 RAG；
        - `context_assembler` 增加 `memory_snippets` 注入位（在摘要与负面案例之间）。
    *   **数据目录**：向量数据与 Mnesia 并列，例如 `bin/run/redis/`（RDB/AOF）或 `data/milvus/`（Milvus 模式）；纳入 `MODE=dev|prod`。
    *   **验收**：① 多轮对话后问「我第一轮说了什么」→ 摘要或 RAG 能召回；② 导入外部文档后问知识性问题 → Agent 自动 `memory_search` 并回答；③ 向量库进程重启后索引可恢复（Redis AOF / Milvus 持久化卷）。
    *   **落地顺序建议**：4.1a Redis Stack + memory 工具 ✅ → 4.1b Erlang 摘要写 Mnesia ✅ → 4.1c RAG 自动注入 ✅ → 4.1d（可选）切 Milvus hybrid。
    *   **4.1c 已实现（RAG 预取）**：
        - `memory_rag.erl`：`thinking(enter)` 前以最后 user 消息调用 `memory_search`（`bridge_manager:call_tool_sync/2`）
        - `context_assembler` 增加 `memory_snippets` 段（位于摘要与负面案例之间）
        - `agent_fsm`：`idle` 启动时从 `state_store` 重载 history（支持 panel 多轮 send）
    *   **4.1b 已实现**：
        - `memory_summarizer.erl`：轮次结束 `schedule/1` → 异步 LLM 摘要 → Mnesia `session_summaries`
        - FSM 最终答案回 `idle` 时触发摘要；`thinking(enter)` 注入最近 3 条摘要
        - `context_assembler` 增加 `session_summaries` 段（位于负面案例之前）
    *   **4.1a 已实现**：
        - Go：`Eion-tools/internal/memory`（Eino Redis Indexer/Retriever + OpenAI/mock Embedder）
        - 工具：`memory_store` / `memory_search` / `memory_import`（`internal/tool/memory.go`）
        - Erlang：`panel_tools` 注册工具描述；`agent_memory_demo` / `agent_memory_recall_demo` 端到端演示
        - 脚本：`scrtps/memory-env.bat`（默认 VM Redis `192.168.59.129:6379`）、`run-redis-integration-test.bat`、`run-memory-tool-test.bat`、`run-memory-demo.bat`、`run-memory-recall-demo.bat`
        - 环境变量：`HERMES_MEMORY_BACKEND=redis`（Windows 默认连 VM Redis Stack）、`HERMES_MEMORY_MOCK_EMBED=1`、`HERMES_EMBEDDING_*`
        - VM 部署：`scrtps/vm-install-docker-redis.sh`、`vm-pull-redis-stack.sh`（DaoCloud 镜像源）
        - 本地 dev 回退：`HERMES_MEMORY_BACKEND=dev`（进程内向量库，无需 Redis）
*   **Step 4.2: Prompt + Context 完善** ✅ *已落地*
    *   **动作**：
        - `prompt_templates.erl`：`core/0` + 五阶段 `phase_hint/1`；
        - `context_assembler`：`filter_llm_history/1`、`trim_history/2`（默认 40 条）、`session_prompt` / `prompt_phase` 注入；
        - `agent_fsm`：`#data.session_prompt`、`detect_prompt_phase/1`、`maybe_schedule_mid_session_summary/2`；
        - `panel_server`：`start_session` 传 `{session_prompt, SystemPrompt}`，system 不再写入 history；
        - `memory_summarizer:schedule_if_long/1`：history ≥ 20 条时中期摘要（session 级去重）。
    *   **验收**：✅ EUnit `prompt_templates_tests` + 扩展 `context_assembler_tests`；`.\make.ps1 test` 全绿。
*   **Step 4.3: Wails v3 混合打包** *部分完成*
    *   **动作**：初始化 Wails v3 项目。`brain.Bridge` 作为 Wails Service 在 `ServiceStartup` 时发现 `panel_server` 地址（读端口文件 `<repo>/bin/run/panel.addr`）并建立连接池。三进程通过 `scrtps/` 下的脚本独立拉起，端口文件做地址握手：UI 模式推荐 **`start-all.bat wails`** / **`make.ps1 start-all-ui`**，分步顺序为 `start-agent.bat` → `start-wails.bat`；独立 tools / CLI 模式使用 `start-tools.bat` → `start-agent.bat` 或 **`start-all.bat`**。停止用 **`stop-all.bat`**。构建：`make.ps1` 提供 `env` / `agent` / `tools` / `wails_v3` / **`start-all`** / **`start-all-ui`** / **`stop-all`** 目标。
    *   **补充**：交付形态按三进程组织：Wails 进程负责 UI，Wails-Go 进程负责桥接与执行，Erlang 进程负责编排。若底层启用 `PanelExec` 回送机制，也仅视为 Wails-Go 与 Erlang 之间的实现细节，不改变三进程部署边界。
    *   **验收**：执行 `wails build`，生成 `.app` 包。双击运行，能看到前端界面，三进程正常通信。
*   **Step 4.4: macOS 签名公证**
    *   **动作**：编写 `postbuild` 脚本。使用 `install_name_tool` 修复 Erlang `.dylib` 的 `@rpath`。执行 `codesign --deep --force` 签名，调用 `notarytool` 提交苹果公证。
    *   **验收**：在全新未受信的 macOS 机器上下载 `.app`，双击运行不被 Gatekeeper 拦截，无“已损坏”提示。
## 5.1 能力平台化现状（已落地）
- **统一能力模型**：前后端已统一消费 capability 元数据，前端能力目录直接展示 `kind / source / streaming / risk_level`，Erlang 不再通过工具名硬编码猜来源。
- **生态接入现状**：当前已接入 11 个 capability，覆盖四类能力样例：
  - `plugin`：`repo_map` / `code_search` / `github_repo_overview` / `github_diff_summary`
  - `mcp`：`codegraph_explore`（本地 Rust 二进制 + SQLite，代码语义图查询）、`mcp_echo`（mock server）
  - `skill`：`workspace_briefing` 组合能力
  - `tool`：基础工具
- **能力裁剪策略**：`capability_selector` + `capability_policy` 在 Erlang 层按上下文 intent 和风险级别做能力过滤，高风险能力直接屏蔽（审批链路落地前不暴露给模型）。
- **MCP 协议**：MCP client 已修复为标准 newline-delimited JSON 协议（含向后兼容 Content-Length 自动检测），可接入标准 MCP server。
- **调试链路**：前端与 panel 侧统一走 `list_capabilities + debug_capability`；能力目录、调试面板、验收脚本共用同一条协议链。
- **回归闭环**：`make.ps1 test` 已纳入 capability 元数据校验与旧 panel 流式链路校验，既验证新字段，也验证旧 request/response/stream 不回归。

### 5.1.1 完成度概览（2026-06-28）

| 版本 | 完成度 | 状态 |
|------|--------|------|
| V0.9 底座 | ~100% | manifest/selector/policy/协议/UI/broker/审批链路 均已通过验收（ACPT-P0-001/002/003/004/005/006，含 approval_store 12 项 eunit + agent_fsm acting 分流 + panel_server approve RPC）；capability selector/policy 5 项 eunit + agent_fsm 集成；execution broker 12 项单测 + panel e2e |
| V1.0 Skill/Plugin | ~85% | skill runtime 已通过验收(ACPT-P1-001)；plugin host 已通过验收(ACPT-P1-002，install/enable/disable/uninstall，9 项单测 + panel e2e 全 PASS)；repo_map/code_search/能力裁剪 均已通过验收(ACPT-P1-003/004/006，panel_capability_e2e 直接调用 + capability_selector 5 项 eunit 全 PASS)；UI trace 仅计数 |
| V1.1 MCP 接入 | 100% | mcp client manager + UI 来源展示 已通过验收 |
| V1.2 GitHub/多模态 | 25% | GitHub 节流已通过；ASR/OCR/文生图待开发 |
| V1.4 codegraph | 100% | codegraph MCP 代码语义图已通过验收 |
| V2.0 规划与治理 | 0% | planner/记忆分层/provider/能力市场/回归矩阵全部未开始 |

**整体代码完成度约 65%**（24 项验收中 8 项通过 + 6 项代码已实现 + 2 项部分实现）。
## 6. 风险提示与防范
1.  **Eino 隐式编排风险**：严格禁止在 Go 侧使用 Eino 的 `agent.NewAgent` 或复杂 `Chain`。仅使用基础 `model.ChatModel` 和 `tools.Tool` 接口，防止控制权泄漏到 Go 侧。
2.  **Protobuf 心智负担**：强制要求 Erlang 业务层只操作 Map，严禁跨层调用 `hermes_pb` / `panel` 模块，违者代码 Review 直接打回。
3.  **panel_server socket 所有权**：accepted socket 的 controlling process 必须是 connection handler；须在 `controlling_process/2` 同步返回 `ok` 后再读写 socket，且 `{packet, 4}` 下 `gen_tcp:send` 只发 Protobuf 负载（勿重复加长度前缀）。
4.  **双重死循环**：Erlang 侧必须设置宏观的 Loop 最大次数限制（如 10 次），防止 LLM 陷入死循环消耗 Token。
5.  **定时事件统一**：所有周期/一次性定时必须经 `timing_wheel`（`erlang:start_timer`）调度，严禁业务模块直接 `erlang:send_after`，避免崩溃后悬挂引用。
6.  **Mnesia 生命周期**：Mnesia 不得写入 `.app.src` 的 applications 列表，其 schema/启动/表创建全部由 `mnesia_store` 管理，避免 RAM-only schema 冲突。数据目录通过 `MODE` 环境变量（dev/prod）或 `MNESIA_DIR` 覆盖。
7.  **`.app.src` 放置**：`hermes_brains.app.src` 必须直接位于 `src/` 目录下（rebar3 不递归扫描 `.app.src`），其余 `.erl` 可按职责分子目录（`src/app/`、`src/agent/` 等）。
