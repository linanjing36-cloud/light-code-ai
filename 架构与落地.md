这里为你准备了两份独立且互补的文档。第一份是**架构设计方案**，侧重于系统拓扑、边界划分与核心机制；第二份是**落地实施流程**，侧重于工程推进的步骤、交付物与风险控制。
这两份文档摒弃了过度设计的华丽辞藻，力求科学、务实，直接指导代码开发。
---
# 文档一：Hermes Agent 架构设计方案
## 1. 架构定位与设计哲学
Hermes Agent 采用 **Erlang/OTP + Go (Eino)** 的混合架构。核心设计哲学是 **“单大脑强控，无状态执行”**：
1. **Erlang 独占编排权**：Erlang 的 `gen_statem` 是系统唯一的控制中心。ReAct 循环的每一个步进、上下文的组装、并发调度与容错降级，全部在 Erlang 侧闭环。
2. **Go/Eino 纯粹执行**：Go 侧引入字节跳动 Eino 框架，但**严格剥夺其编排能力**。Eino 仅作为对接大模型 API 和工具生态的 SDK 适配器。Go 侧不保存状态，不内部循环，做到“问什么答什么”。
3. **二进制契约通信**：全链路采用 Protobuf 通信，仅在 Go 侧最靠近 LLM API 的边缘地带转换为 JSON，建立坚固的防腐层。
## 2. 系统拓扑与职责边界
### 2.1 Erlang 侧 (编排大脑)
- **`Agent_Sup`**：`simple_one_for_one` 监督者，动态派发 Agent 进程，负责进程级崩溃重启。
- **`Agent_FSM`**：核心状态机。维护会话生命周期，执行 ReAct 循环流转。
- **`Context_Assembler`**：上下文组装器。负责从 ETS/Mnesia 提取历史记忆、注入系统提示词、裁剪工具列表，组装出完整的请求包。
- **`Bridge_Manager`**：通信网关。维护与 Go 节点的连接池，监控 Go 节点存活，处理请求超时与断线兜底。
- **`pb_codec`**：Protobuf 防腐层。业务层只操作 Erlang Map，由该模块负责与 Protobuf Binary 的互转，并消化 `google.protobuf.Struct` 等动态类型。
### 2.2 Go 侧 (执行肌肉 - 基于 Eino)
- **`Command_Dispatcher`**：接收 Erlang 的 Protobuf 二进制流，解码后路由给具体的执行器。
- **`Eino_Model_Wrapper`**：调用 Eino 的 `model.ChatModel` 接口。将 Erlang 传来的请求转为 API 格式，调用 LLM，解析响应后封装为 Protobuf 发回 Erlang。
- **`Eino_Tool_Wrapper`**：调用 Eino 的 `tools.Tool` 接口。执行具体的工具函数（如 HTTP 请求），返回结果。
- **`Panic_Guard`**：全局异常拦截器。捕获 Go 侧执行过程中的 Panic，防止进程崩溃，将异常转为错误响应发回 Erlang。
## 3. 通信基座设计：Protobuf 防腐层
跨语言通信采用细粒度的原子请求，确保 Go 侧无法自主决定后续动作。
### 3.1 核心协议定义
- **`LLMInferRequest`**：纯模型推理请求。包含 Erlang 拼装好的完整 `messages` 和 `tools` 描述。
- **`ToolExecRequest`**：纯工具执行请求。包含 `tool_name` 和动态 `arguments`（使用 `Struct` 类型）。
### 3.2 Erlang 侧 Map 隔离
业务逻辑层**严禁**直接操作 Protobuf 生成的结构。`pb_codec` 模块提供透明转换：
- 开启 `gpb` 的 `use_maps, true` 选项。
- `pb_codec` 内部实现 `to_struct/1` 和 `from_struct/1`，自动将普通 Erlang Map 与 Protobuf 的 `Struct` 格式互转。
- **效果**：`Agent_FSM` 永远只处理 `#{model => <<"gpt-4">>, messages => [...]}`，完全感知不到 Protobuf 的存在。
## 4. 三层工程落地机制
### 4.1 Loop Engineering (循环工程) - Erlang 主导
`Agent_FSM` 维持严格的状态流转：
- **`thinking`**：向 Go 发送 `LLMInferRequest`，等待 LLM 决策。
- **`acting`**：若决策包含工具调用，Erlang **并行**向 Go 发送多个 `ToolExecRequest`。
- **`observing`**：收集所有工具结果，拼接进上下文，循环回 `thinking` 或输出最终结果。
### 4.2 Context Engineering (上下文工程) - Erlang 主导
- **短期状态**：Erlang ETS 存储当前状态机快照与最近 N 轮原始对话。
- **中期记忆**：Erlang Mnesia 存储更早的会话历史摘要。
- **组装流水线**：`Context_Assembler` 在每次 `thinking` 前，按优先级组装完整上下文发往 Go 侧。
### 4.3 Prompt Engineering (提示词工程) - Erlang 主导
Erlang 侧维护提示词模板系统，根据状态机当前阶段（首轮对话、工具报错反思等）动态拼接 System Prompt，确保 LLM 输出格式符合预期。
## 5. 容错与降级策略
1. **进程级容错**：Agent 进程崩溃，Erlang Supervisor 毫秒级重启，从 ETS 快照恢复状态。Go 侧无状态，重启不影响全局。
2. **通信层兜底**：Go 节点 `nodedown` 时，`Bridge_Manager` 立即将未完成请求标记为 `interrupted`。
3. **柔性降级**：中断或工具失败不直接结束流程，而是作为特殊 Observation 喂给 LLM，由 LLM 决定道歉、重试或换路径。
4. **幂等性保障**：Erlang 生成全局 `ReqId`。Go 侧执行带副作用工具前，基于 `ReqId` 查去重缓存，防止重试导致脏数据。
---
# 文档二：Hermes Agent 落地实施流程文档
# Hermes Agent 落地实施流程文档
## 1. 实施总则
本流程按“先契约后逻辑，先单点后闭环，先跑通后容错”的科学路径推进。每个阶段设定明确的交付物与验收标准，确保代码增量可控，避免后期大规模返工。
## 2. 阶段一：通信基座与单点验证 (Phase 1)
**目标**：打通 Erlang <-> Go 的二进制通信基座，验证 Eino 作为纯执行 SDK 的可用性。
*   **Step 1.1: 定义 Protobuf 契约**
    *   **动作**：建立项目级 `proto/hermes.proto` 文件。定义 `AgentRequest` 和 `AgentResponse` 包装体，内部使用 `oneof` 区分 `LLMInferRequest` 和 `ToolExecRequest`。动态参数统一使用 `google.protobuf.Struct`。
    *   **验收**：`.proto` 文件通过 `protoc` 编译无报错。
*   **Step 1.2: Erlang 防腐层落地**
    *   **动作**：在 `rebar.config` 中引入 `gpb` 插件，配置 `use_maps, true` 和 `strings_as_binaries, true`。编写 `pb_codec.erl` 模块，实现 `to_struct/1` 和 `from_struct/1` 辅助函数。
    *   **验收**：编写 EUnit 测试用例，传入一个包含嵌套 Map 的 Erlang 数据结构，`encode_req` 后立即 `decode_req`，能还原出完全相等的 Map。
*   **Step 1.3: Go 侧 Eino 适配与单点联调**
    *   **动作**：初始化 Go 项目，引入 Eino SDK。编写 `Command_Dispatcher` 监听 Erlang 节点消息。实现 `Eino_Model_Wrapper`，调用 Eino 的 `model.ChatModel` 接口对接 OpenAI API。
    *   **验收**：Erlang 端构造一个纯文本问答 Map -> 编码发给 Go -> Go 调通 LLM API -> 返回结果解码给 Erlang -> Erlang 控制台打印出 LLM 的回答。
## 3. 阶段二：Erlang 核心状态机闭环 (Phase 2)
**目标**：在 Erlang 侧实现完整的 ReAct 循环逻辑，跑通带工具调用的单会话 Agent。
*   **Step 2.1: 状态机骨架构建**
    *   **动作**：基于 `gen_statem` 实现 `agent_fsm.erl`。定义 `idle`、`thinking`、`acting`、`observing` 四个状态。实现基础的状态迁移事件回调。
    *   **验收**：可以通过手动发送事件，驱动状态机在四个状态间空转，不报错。
*   **Step 2.2: 上下文组装器实现**
    *   **动作**：实现 `context_assembler.erl`。提供接口将 System Prompt、最近对话历史、可用工具描述拼接成符合 LLM API 规范的 Messages 列表。
    *   **验收**：组装后的 Map 结构通过 `pb_codec` 编码，Go 侧解码后能直接作为 Eino `ChatModel` 的入参，无需二次加工。
*   **Step 2.3: 并行工具调度与闭环**
    *   **动作**：在 Go 侧实现 `Eino_Tool_Wrapper`，注册一个测试工具（如 `get_weather`）。在 Erlang `acting` 状态下，解析 LLM 返回的 `tool_calls`，使用 `erlang:send_after` 或 `async` 模式并行派发 `ToolExecRequest` 给 Go，并收集所有结果。
    *   **验收**：用户提问“北京天气如何”，Agent 能自动调用 Go 侧的天气工具，并将结果反馈给 LLM，最终输出正确的自然语言总结。ReAct 循环跑通。
## 4. 阶段三：容错机制与状态恢复 (Phase 3)
**目标**：达到生产级可用性，解决崩溃、断线、死循环等异常情况。
*   **Step 3.1: Erlang 进程级崩溃恢复**
    *   **动作**：设计 `state_store` 进程，持有系统级 ETS 表。`Agent_FSM` 在每次状态迁移前，将当前上下文和状态机阶段快照写入 ETS。配置 `Agent_Sup` 为 `rest_for_one` 或 `simple_one_for_one` 重启策略。
    *   **验收**：在 Agent 正在等待工具返回时，手动 `exit(Pid, kill)` 杀死 `Agent_FSM`。Supervisor 重启进程后，Agent 能从 ETS 恢复断点，重新发起请求或继续流程。
*   **Step 3.2: 通信层容错与柔性降级**
    *   **动作**：完善 `Bridge_Manager`。监控 Go 节点 `nodedown` 事件。一旦断线，立即向所有 pending 的 `Agent_FSM` 发送 `{bridge_disconnect}` 消息。`Agent_FSM` 收到后，生成特殊 Observation 喂给 LLM 进行降级处理。
    *   **验收**：在 Agent 执行多步任务中途，强制杀掉 Go 进程。Erlang 侧不崩溃，LLM 能感知到工具不可用并尝试换用其他工具或向用户致歉。
*   **Step 3.3: Go 侧 Panic 防护与幂等**
    *   **动作**：Go 侧 `Command_Dispatcher` 增加 `defer/recover` 中间件。Erlang 侧生成全局 `ReqId` 随请求下发。Go 侧执行带副作用工具前，基于 `ReqId` 查询本地 LRU 缓存。
    *   **验收**：Go 侧工具模拟 Panic，Erlang 侧收到结构化 `ErrorResponse` 而非连接断开。Erlang 侧重发相同 `ReqId` 的请求，Go 侧直接返回缓存结果，不执行二次副作用。
## 5. 阶段四：长期记忆与产品化打包 (Phase 4)
**目标**：补齐 RAG 能力，完成 Mac 桌面端应用交付。
*   **Step 4.1: 长期记忆集成**
    *   **动作**：Erlang 侧引入 Mnesia，存储超出短期 Context 窗口的历史摘要。Go 侧利用 Eino 集成本地向量库（或对接外部 API），将其封装为一个特殊的 `Tool` 暴露给 Erlang 调度。
    *   **验收**：多轮长对话后，Agent 依然能记住早期关键信息；用户提问外部知识，Agent 能自动调用 RAG 工具检索并回答。
*   **Step 4.2: Wails 混合打包**
    *   **动作**：初始化 Wails 项目。编写 Go 启动脚本，通过 `os/exec` 拉起内嵌的 Erlang ERTS 子进程，重定向数据目录到 `~/Library/Application Support/Hermes/`。配置 ErlPort 双向连接建立逻辑。
    *   **验收**：执行 `wails build`，生成 `.app` 包。双击运行，能看到前端界面，后台 Erlang 和 Go 正常通信。
*   **Step 4.3: macOS 签名公证**
    *   **动作**：编写 `postbuild` 脚本。使用 `install_name_tool` 修复 Erlang `.dylib` 的 `@rpath`。执行 `codesign --deep --force` 签名，调用 `notarytool` 提交苹果公证。
    *   **验收**：在全新未受信的 macOS 机器上下载 `.app`，双击运行不被 Gatekeeper 拦截，无“已损坏”提示。
## 6. 风险提示与防范
1.  **Eino 隐式编排风险**：严格禁止在 Go 侧使用 Eino 的 `agent.NewAgent` 或复杂 `Chain`。仅使用基础 `model.ChatModel` 和 `tools.Tool` 接口，防止控制权泄漏到 Go 侧。
2.  **Protobuf 心智负担**：强制要求 Erlang 业务层只操作 Map，严禁跨层调用 `hermes_pb` 模块，违者代码 Review 直接打回。
3.  **双重死循环**：Erlang 侧必须设置宏观的 Loop 最大次数限制（如 10 次），防止 LLM 陷入死循环消耗 Token。

